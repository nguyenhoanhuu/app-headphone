import React from 'react';
import { Text, View } from 'react-native';

const ProductInfo = () => {
    return (
        <View>
            <Text>UI</Text>
        </View>
    );
};

export default ProductInfo;
